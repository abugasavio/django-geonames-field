============
Installation
============

At the command line::

    $ easy_install django-geonames-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-geonames-field
    $ pip install django-geonames-field
